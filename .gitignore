# Reference material stays OFF the cloud by default: it contains the master
# workbooks (50+ years of real survey data) and internal protocols.
# Add explicitly if that's ever wanted.
/reference material/

# Built APKs are ~130 MB each — over GitHub's 100 MB file limit, and
# retrievable from EAS anytime. Keep local copies, not repo copies.
*.apk

# Scratch verification screenshots from the debug loop
/launcher-*.png
/tablet-*.png
/quickcard-preview.png

# Local build archive — stays on this machine only
/versions/
